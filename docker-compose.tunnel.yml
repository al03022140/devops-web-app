version: '3.8'

services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: devops-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate --url http://nginx:80
    depends_on:
      - nginx

